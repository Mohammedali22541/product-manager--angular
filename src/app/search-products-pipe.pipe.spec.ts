import { SearchProductsPipePipe } from './search-products-pipe.pipe';

describe('SearchProductsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProductsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
